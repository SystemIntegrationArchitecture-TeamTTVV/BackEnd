spring.application.name=AuthService

server.port=${PORT:8083}
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never

# --- PostgreSQL (Supabase) ---
# ⚠️ PRODUCTION: set DB_URL, DB_USER, DB_PASSWORD env vars
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
spring.datasource.username=${DB_USER:postgres.pogzbnvnzuhmmadhkryl}
spring.datasource.password=${DB_PASSWORD:VoSJaMsFmV2PSx3j}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

app.security.enabled=false
app.data.seed.enabled=${APP_DATA_SEED_ENABLED:false}

springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ─── Brevo SMTP ────────────────────────────────────────────────────────────
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${BREVO_SMTP_LOGIN:a7f761001@smtp-brevo.com}
spring.mail.password=${BREVO_SMTP_KEY:xsmtpsib-5209cae16188f19334f207e1881b074977797e74de7e4d7cb947dc86c7a41857-UrJ21bJBfny0W8Ml}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
mail.from=${MAIL_FROM:TTVV@Business.com.vn}
mail.from-name=${MAIL_FROM_NAME:TTVV Business}
app.base-url=${APP_BASE_URL:http://localhost:5173}

jwt.secret=${JWT_SECRET:mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLong}
jwt.access-token-expiration=${JWT_ACCESS_TOKEN_EXPIRATION:1800000}
jwt.refresh-token-expiration=${JWT_REFRESH_TOKEN_EXPIRATION:2592000000}

app.token-store.enabled=${APP_TOKEN_STORE_ENABLED:true}
app.features.device-sessions.enabled=${APP_FEATURE_DEVICE_SESSIONS_ENABLED:false}
app.features.blocking.enabled=${APP_FEATURE_BLOCKING_ENABLED:false}
app.features.user-status.enabled=${APP_FEATURE_USER_STATUS_ENABLED:false}
app.features.data-export.enabled=${APP_FEATURE_DATA_EXPORT_ENABLED:false}
app.features.two-factor.enabled=${APP_FEATURE_TWO_FACTOR_ENABLED:false}
spring.data.redis.host=${REDIS_HOST:47.129.208.120}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:123456}
spring.data.redis.database=0
spring.data.redis.timeout=3000

app.kafka.enabled=${APP_KAFKA_ENABLED:false}
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.admin.auto-create=${app.kafka.enabled:false}
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always
management.health.redis.enabled=false
